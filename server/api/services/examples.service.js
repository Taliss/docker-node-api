import { v4 as uuidv4 } from 'uuid';

// we are using User in the function names just for convention here - team decision.
// const createUser = async (user) => {
//   return db.create({
//     id: uuidv4(),
//     ...user,
//     created: 'DATE, blah',
//   });
// };

// const getUser = (email) => {
//   return db.findUser(email);
// };

// const deleteUser = (id) => {
//   return db.delete(id);
// };

// const updateUser = (data) => {
//   return db.update(data);
// };

export default {
  // getUser,
  // createUser,
  // deleteUser,
  // updateUser,
};
